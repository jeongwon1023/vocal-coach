# 정밀 분석 (로컬 권장 — Streamlit Cloud는 메모리 제한)
# pip install -r requirements-precision.txt

crepe>=0.0.16
tensorflow>=2.15.0,<2.17.0
demucs>=4.0.0
torch>=2.1.0

# 환경 변수 (선택)
# USE_CREPE=1          # CREPE F0 (기본 켜짐, crepe 설치 시)
# VOCAL_SEP=auto       # auto | enhanced | demucs
